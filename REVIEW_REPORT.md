# REVIEW_REPORT — multi_ai_cli 설계 검토 수렴

- 일시: 2026-09-03
- 대상: `INTENT.md`, `SPEC.md`
- 검토자: codex(GPT-5 Codex), agy(Gemini 3.8 Flash High)
- 수렴자: Claude
- 라운드: 1
- 원본: `review/codex_review.md`, `review/agy_review.md`

## 0. 판정 요약

| 검토자 | 판정 | 핵심 우려 |
|---|:---:|---|
| codex | CONDITIONAL | `.cmd` 셸 경로 제거, 명령행 길이 처리, 손실 없는 채팅 저장 형식 |
| agy | CONDITIONAL | Windows 명령행 32KB 한계에 따른 프롬프트 전달 방식 전환 |

**종합: 일치.** 양쪽 모두 `CONDITIONAL` 이고, **막는 사유까지 동일**하다 — Windows 명령행 길이 한계와 `codex.cmd` 셸 경유. 서로 못 본 상태에서 같은 두 지점을 짚었다.

---

## 1. 의도 전달 검증

문서 구조 개편(F11·F12)이 통했는지 판정한다.

| 항목 | codex | agy |
|---|:---:|:---:|
| 최종 목표를 Phase 3 수렴으로 짚었는가 | **O** | **O** |
| Non-goals를 인식했는가 | O | **O** (구독 인증·역할 고정 배제를 명시) |
| 라운드 격리(동조 방지) 메커니즘을 이해했는가 | **O** | **O** |
| `ARCHIVE.md` 를 근거로 든 흔적 | 없음 | 없음 |

**판정: 성공.**

codex는 "핵심 목표는 단순 병렬 호출이 아니라 (…) 사용자가 결정할 쟁점만 남기는 것", agy는 "'구조화 수렴(Phase 3)' 과정을 자동화하는 것 (…) 을 결승선으로 정의한다"고 썼다. 둘 다 Phase 1을 목적으로 오인하지 않았고, `ARCHIVE.md` 의 폐기된 `tri-review` 를 근거로 든 흔적이 없다.

### 양쪽이 공통으로 지적한 문서 결함 — `INTENT.md`/`SPEC.md` 수정 대상

**두 검토자가 독립적으로 같은 구멍을 짚었으므로 실제 결함이다.**

| # | 공통 지적 | 조치 |
|:---:|---|---|
| **G1** | **Phase 3 수렴의 실행 주체가 정의돼 있지 않다.** codex: "누가 분류·수렴을 수행하며 어떤 조건으로 합의와 미해결을 판정하는지 명시되지 않았다." agy: "'수렴자(Consolidator)' 역할을 세 AI 중 특정 모델이 전담하는지, 외부 규칙 기반 엔진인지 (…) 명시가 없다." | `SPEC.md` §7에 수렴자 실행 계약을 선제 정의. `prompts/consolidate.md` 는 절차만 있고 스펙에 반영이 안 돼 있었다 |
| **G2** | **임의 길이 문맥을 명령행 인자로 전달할 수 있다는 전제가 검증 없이 깔려 있다.** codex가 A섹션에서 "숨은 전제"로 지목했고, C섹션에서 양쪽이 critical로 재지적 | §2 R1 참조. **실측으로 확정됨** |
| **G3** | **Markdown transcript에서 임의 AI 출력을 손실 없이 재구조화할 수 있다는 전제.** codex A섹션 + C섹션, agy C섹션 | §2 R3 참조 |

### 한쪽만 지적한 누락 전제 (agy)

기록만 하고 Phase 1 범위에서는 다루지 않는다.

- **쿼터·레이트리밋 고갈 대응**: 구독 인증이라 과금은 없지만 호출 한도는 있다. 초과 시 재시도·대체 라우팅 규약이 없다.
- **응답 시간 편차**: Flash(수 초)와 고추론(수 분)이 섞이면 대기 중 사용자 인터랙션 허용 범위가 정의돼 있지 않다.
- **읽기 전용 모드의 격리 수준**: `read-only`/`plan` 이 워크스페이스 쓰기만 막는지, `~/.ssh` 같은 민감 파일 읽기까지 막는지 공급자별로 같다고 암묵 전제돼 있다.

---

## 2. 새로 발견된 문제 — 수렴 결과

### 2.1. 합의 — 양쪽이 같은 쟁점을 제기

#### R1. [critical · 확인함] Windows 명령행 길이 한계가 §5.3 문맥 상한과 충돌

- **codex**: "약 40,000자의 문맥에 (…) 하나의 인자로 붙이면 Windows `CreateProcess` 명령행 한도를 초과할 수 있다."
- **agy**: "`CreateProcessW` 의 전체 커맨드라인 길이 한계는 32,767자이며 (…) `CreateProcess error=206` 예외가 발생하며 프로세스 생성이 원천 실패한다."

**수렴자 실측 (2026-09-03, JDK 17.0.2 + `ProcessBuilder` → `agy.exe`)**

| 인자 길이 | 결과 |
|---:|---|
| 32,000자 | OK |
| 32,600자 | OK |
| 33,000자 | **FAIL — `CreateProcess error=206`** |
| 40,000자 | FAIL |

**확정.** 이론이 아니라 이 환경에서 재현된다. `SPEC.md` §5.3의 "약 40,000자"는 **프로세스 기동 자체를 실패시킨다.**

**공급자별 stdin 우회 가능 여부 — 수렴자 실측**

| CLI | stdin 프롬프트 | 근거 |
|---|:---:|---|
| `codex` | **가능** | `codex exec --help`: "If not provided as an argument (or if `-` is used), instructions are read from stdin". `echo ... \| codex exec -` 로 `CODEX_STDIN_OK` 응답 확인 |
| `claude` | **가능** | `echo ... \| claude -p` 로 `CLAUDE_STDIN_OK` 응답 확인 |
| `agy` | **불가능** | `-p` 가 값을 요구하는 플래그다. `-p=-` 은 `-` 를 리터럴 프롬프트로 처리해 stdin을 무시했다(무관한 인사말 반환). `-p=` 는 `empty prompt` 오류. text 모드에서 stdin 경로 없음 |

**agy만 우회로가 없다.** 남은 선택지는 두 개다.

1. `--input-format stream-json` — stdin에서 NDJSON을 읽는다. 단 `--output-format stream-json` 을 강제하므로 **JSON 파싱이 필요해져 Q1 결정과 충돌**한다.
2. **문맥 상한을 명령행 한계 아래로 낮춘다.** agy가 제안한 16,000자가 합리적이다.

**추가 문제**: `SPEC.md` §6.3은 "프로세스 시작 직후 stdin을 닫아 입력 대기 행을 방지한다"고 규정한다. codex·claude의 stdin 우회로를 쓰려면 **이 규칙과 정면 충돌**한다. 규칙을 "프롬프트 주입 후 닫는다"로 고쳐야 한다.

#### R2. [critical · 확인함] `codex.cmd` 를 `%ComSpec%` 로 경유하면 셸 경계가 무너진다

- **codex**: "`ProcessBuilder` 에 배열로 전달해도 `%ComSpec% /c` 이후의 인수는 `cmd.exe` 가 다시 파싱한다. (…) '프롬프트를 셸 문자열에 연결하지 않는다'는 보안 경계가 성립하지 않는다."
- **agy**: "`cmd.exe` 는 `&`, `|`, `<`, `>`, `^`, `%` 등의 셸 메타문자를 해석한다. 프롬프트에 소스 코드(`&&`, `<vector>`, `%VAR%`)가 포함되면 명령어가 의도치 않게 분할 실행되거나 크래시된다."

양쪽 다 대체안까지 일치한다 — **`.cmd` 경유를 탐색 체인에서 제거**하고 네이티브 `codex.exe` 또는 `node.exe` + `codex.js` 를 직접 실행한다.

이 지적은 `SPEC.md` §6.3의 자기모순을 드러낸다. 같은 절이 "프롬프트를 하나의 셸 명령 문자열에 연결하지 않는다"고 해놓고 `.cmd` 폴백을 허용했다.

> **수렴자 주**: §6.2에 이미 네이티브 `codex.exe` 실측 경로가 기록돼 있고 `codex-cli 0.152.1` 로 동작을 확인했다. 폴백 체인에서 `.cmd` 를 빼도 잃는 게 없다.

#### R3. [major] `transcript.md` 만으로는 채팅 기록을 손실 없이 재개할 수 없다

- **codex** (major): "AI 출력에는 transcript의 헤딩·구분자·코드 펜스와 동일한 문자열이 들어갈 수 있지만 메시지 경계의 이스케이프 또는 기계 판독 형식이 정의되지 않았다. 따라서 D6의 SSOT와 '채팅방 저장·재개' 완료 기준을 신뢰성 있게 구현할 수 없다."
- **agy** (minor): 같은 문제. 대안으로 `messages.properties` 병행 또는 `<!-- msg:start id=... -->` HTML 주석 경계 제안.

**심각도는 codex의 major를 채택한다.** `D6`(채팅방 기록이 SSOT)과 §7.10 완료 기준("종료 후 채팅방을 다시 열어 이전 대화를 확인할 수 있다")이 직접 걸려 있다.

수렴자 판단으로는 agy의 HTML 주석 경계가 codex의 길이 접두어 레코드보다 낫다. **`transcript.md` 하나로 사람이 읽는 용도와 기계 판독을 동시에 만족**시키고 외부 라이브러리도 필요 없다(`D12` 유지).

#### R4. [major] Phase 3 수렴자 아키텍처 결손 — G1과 동일

양쪽이 A섹션에서 지적했고 agy는 C섹션에서 critical의 일부로 다시 올렸다. §3.1의 조치로 처리한다.

### 2.2. 이견 — 판단이 상충

**없음.** 두 리뷰가 정면으로 충돌하는 항목은 없었다. 겹치는 지적은 전부 방향이 일치했다.

### 2.3. 단독 지적

#### codex 단독

| 심각도 | 요약 | 위치 | 2R 대상 |
|---|---|---|:---:|
| major | **`ProcessHandle` 만으로 트리 종료를 보장 못 한다.** descendants 열거와 종료 사이에 새 자식이 생기거나 자식이 먼저 분리되면 좀비가 남는다. §8.4 K4의 "완벽 대응 가능" 판정에 종료 확인·재시도·경쟁 조건 처리가 없다 | §6.3, §7.7, §8.4 K4 | **O** |
| major | **문맥 절단이 Phase 3 증거를 제거한다.** 최근 12개/40,000자 정책에서는 1라운드 답변과 반론이 길면 최초 안건이 밀려난다. 전체 증거에 대해 분류한다는 최종 목표에 도달 불가 | §5.3, §7.9 | **O** |
| major | **Phase 1 명령 범위가 문서 내부에서 불일치.** §5.2 MVP 목록에 `/cancel`·`/rooms`·`/open` 이 없는데 §7.9·§7.10은 취소와 저장·재개를 Phase 1 완료 조건으로 요구 | §5.2, §7.9, §7.10 | — |
| major | **대상 워크스페이스 결정 규칙이 없다.** `-C`·`--add-dir`·프롬프트가 같은 workspace를 필요로 하는데 현재 디렉터리/설정값/방별 값 중 무엇인지 미정 | §5.3, §7.2, §7.8 | — |

> §5.2·§7.9·§7.10 불일치는 수렴자가 재확인했다. **codex 지적이 맞다.** §5.2는 "MVP에서는 일반 채팅, 멘션, `/status`, `/new`, `/exit` 를 먼저 구현한다"이고, §7.9 Phase 1에는 "타임아웃, 취소"와 "채팅방 Markdown 저장·재개"가, §7.10에는 "`/cancel` 이 대상 프로세스를 종료한다"가 완료 기준으로 들어 있다.

#### agy 단독

| 심각도 | 요약 | 위치 | 2R 대상 |
|---|---|---|:---:|
| major | **`PromptContextBuilder` 가 단일 선형 모델이라 2라운드 비대칭 문맥을 만들 수 없다.** 라운드 상태(`INDEPENDENT_ROUND_1`/`REBUTTAL_ROUND_2`/`CONSOLIDATION`)별로 다르게 조립하는 라운드 인식형으로 확장 필요 | §5.3, §7.1, §7.4 | **O** |
| major | **codex `-o` 임시 파일이 `read-only` 샌드박스와 충돌할 수 있다.** 워크스페이스 내부에 만들면 쓰기가 차단돼 실행 실패. 취소·비정상 종료 시 파일 방치 또는 불완전 기록 | §7.2, §7.6 | **O** |
| minor | **Windows 콘솔 CP949로 한글 렌더링이 깨질 수 있다.** 스트림 디코딩은 UTF-8로 지정했으나 콘솔 출력 코드페이지는 별개 | §5.1, §6.3, §7.1 | — |

> **`-o` 샌드박스 충돌은 미검증이다.** `-o` 는 샌드박스 안 에이전트가 아니라 CLI 하네스가 쓰는 것이라 통과할 가능성이 있다. 다만 **임시 파일을 `%USERPROFILE%\.multi-ai-cli\temp\` 로 옮기는 비용이 거의 없으므로** 검증 없이 채택해도 손해가 없다. `try-finally` 정리와 stdout 폴백도 마찬가지다.

### 2.4. 기각

#### [기각] agy — "Claude CLI는 스키마 강제 플래그가 전무하다"

agy는 이것을 **critical**로 올렸다.

> "§1.5 실측표에서 확인되듯 Codex(`--output-schema`)와 agy(`--json-schema`)와 달리 Claude CLI는 스키마 강제 플래그가 전무하며, `--output-format json` 은 단순 텍스트 래퍼에 불과하다."

**사실이 아니다.** 수렴자가 `claude --help` 로 확인했다.

```
--json-schema <schema>    JSON Schema for structured output validation.
                          Example: {"type":"object","properties":{...},"required":[...]}
```

Claude 2.1.259에 `--json-schema` 가 존재하며 codex·agy와 동등하게 스키마를 강제할 수 있다. 이 지적에서 파생된 "Claude 어댑터에 프롬프트 레벨 Few-shot 스키마 강제를 필수 탑재" 제안도 **함께 기각**한다.

> **다만 원인은 우리 문서에 있다.** `SPEC.md` §1.5의 구조화 출력 표에 codex와 agy만 있고 Claude 행이 없다. agy는 그 표를 근거로 "없다"고 추론했다. **표의 누락이 오판을 유도했으므로 §1.5에 Claude 행을 추가한다.**

같은 항목에서 함께 제기된 "수렴자 아키텍처 결손"은 **유효하며 R4로 채택**한다.

---

## 3. Q1~Q6 수렴

| # | codex | agy | 분류 | 확정 |
|:---:|---|---|---|---|
| Q1 | 동의 | 동의 | **합의** | **확정 — 권장안 채택** |
| Q2 | 조건부 동의 | 동의 | **합의 + 권장안 결함 발견** | **확정 — 권장안 수정 후 채택** |
| Q3 | 동의 | 동의 | **합의** | **확정 — 권장안 채택** |
| Q4 | 정보부족 | 조건부 동의 | 정보부족 | **사용자 결정 필요** |
| Q5 | 조건부 동의 | 조건부 동의 | **합의 (대안이 상호보완)** | **확정 — 양쪽 대안 모두 채택** |
| Q6 | 정보부족 | 동의 | 불완전 합의 | **사용자 결정 필요** |

### Q1. MVP JSON 파싱과 D12 충돌 → **확정: 권장안 채택**

- **codex**: 동의 — "Java 17 표준 라이브러리만으로 (…) 파서를 직접 구현해야 하며, 이는 MVP 범위를 불필요하게 확대한다."
- **agy**: 동의 — "이스케이프 문자 및 개행 처리에서 심각한 결함을 유발하기 쉽다."
- **확정**: MVP는 Claude·agy 모두 `--output-format text`. JSON은 Phase 3으로 미룬다.

> **주의**: R1에서 agy의 유일한 stdin 우회로가 `--input-format stream-json` 이고 이는 JSON 파싱을 요구한다. Q1 확정과 충돌하므로 **agy는 문맥 상한 축소로 해결한다.**

### Q2. 타임아웃 강제 주체 → **확정: 권장안 수정 후 채택**

- **codex**: 조건부 동의 — **권장안 내부 모순 지적.** "권장안의 'agy 타임아웃을 Java보다 짧게 두지 않는다'와 예시인 'agy 10분, Java 11분'은 서로 반대다."
- **agy**: 동의 — 계층형 설계에 이견 없음.

**codex 지적이 맞다.** 수렴자가 §9.2 Q2 원문을 재확인했다. 규칙은 "agy를 Java보다 짧게 두지 않는다"인데 예시 `agy 10m, Java 11m` 은 agy(600초)가 Java(660초)보다 **짧다.** 예시가 규칙을 위반한다.

- **확정**: 타임아웃 주체는 Java로 일원화. **Java 600초, agy `--print-timeout 11m`(660초).** CLI 타임아웃은 Java 감시가 실패했을 때만 작동하는 보조 안전장치다. 잘못된 예시를 수정한다.

### Q3. Claude `--restricted` 채택 → **확정: 채택**

- **codex**: 동의 — "세 공급자가 애플리케이션이 만든 동일 문맥에서 출발하려면 Claude에만 외부 설정이 주입되는 경로를 닫아야 한다."
- **agy**: 동의 — "은닉 프롬프트가 주입되는 것과 같으므로 (…) 엄격히 차단해야 한다."
- **확정**: 읽기 전용 프로필에 `--restricted` 포함.

### Q4. 프로젝트 위치와 버전관리 → **사용자 결정 필요**

- **codex**: 정보부족 — "소스 루트로 현재 디렉터리를 사용할지 별도 `multi_ai_cli` 디렉터리를 만들지, Git 저장소를 생성할지에 대한 사용자 결정이 없다."
- **agy**: 조건부 동의 + 구체안 — "현재 작업 디렉터리에서 `git init` 을 수행하여 저장소 루트로 확정하고 §7.1 구조를 현재 루트 바로 아래 배치. 설계 문서와 Java 코드를 단일 Git 저장소에서 일원화."

agy 안에 반대 근거가 없고 codex도 "사용자 결정이 없다"고만 했다. **§4로 올린다.**

### Q5. Claude 중첩 실행 → **확정: 양쪽 대안 모두 채택**

- **codex**: 조건부 동의 — "`--restricted` 는 설정 상속을 줄이지만 중첩 실행의 사용량, 부모 세션 종료, 프로세스 트리 혼동을 해결하지 않는다. 경고를 안전성 보장으로 취급해서는 안 된다." 대안: 비차단 경고 + 자식 PID·실행 ID 독립 관리.
- **agy**: 조건부 동의 — "부모로부터 상속되는 환경변수(`CLAUDECODE` 등)까지 격리하지는 못한다." 대안: 자식 기동 시 환경변수 맵에서 Claude Code 세션 식별 변수를 명시적으로 제거(Clean Environment).

**두 대안이 상호보완적이다.** 겹치지 않으므로 셋 다 채택한다.

### Q6. agy 기본 모델 → **사용자 결정 필요**

- **codex**: 정보부족 — "모델 목록만으로 품질·지연·구독 한도·컨텍스트 크기가 적합한지 판단할 수 없다. 후보 모델의 실제 응답 품질과 지연 측정, 사용자가 정확도와 속도 중 무엇을 우선하는지 알아야 한다."
- **agy**: 동의 — "Claude(Thinking)와 Codex(high)에 대등하게 대응하려면 Gemini도 고추론 모델이어야 한다."

**참고 자료 하나**: 이번 검토에서 agy는 `gemini-3.8-flash-high` 로 실행됐고, **Flash 등급으로도 critical 2건을 정확히 짚었다.** 다만 Claude `--json-schema` 오판도 같은 실행에서 나왔다. 표본 1회로는 결론을 못 낸다. **§4로 올린다.**

---

## 4. 사용자 결정 — 완료 (2026-09-03)

| # | 안건 | **결정** |
|:---:|---|---|
| **U1** | 프로젝트 위치·버전관리 | **현재 폴더를 git 저장소 루트로 확정.** `src/`·`scripts/` 를 루트 바로 아래 배치 → `SPEC.md` `D14` |
| **U2** | agy 기본 모델 | **`gemini-3.1-pro-high`.** `config.properties` 로 덮어쓰기 가능 → `D13` |
| **U3** | agy 문맥 전달 방식 | **전 공급자 공통 16,000자.** 차등 없음 → `D15` |
| **U4** | 2라운드 실행 | **실행.** 착수 조건은 세 검토자 전원 "수정할 부분 없음" |

> **U3에 대한 보충** — 수렴자가 §4 표에서 (a) 공통 축소를, 요약 보고에서 (c) 공급자별 차등을 각각 권장해 표기가 엇갈렸다. **(a)로 확정한다.**
> 참여자마다 다른 양의 문맥을 받으면 세 답의 차이가 모델 차이인지 입력 차이인지 구분할 수 없어 교차검증 자체가 성립하지 않는다(`INTENT.md` §5). 상한은 가장 제약이 큰 agy에 맞추고, **전송 방식만** 공급자별로 다르게 한다(codex·claude는 stdin, agy는 인자). 내용이 같으므로 동일 조건은 유지된다.

### 4-1. 원래 선택지 (기록)

#### 선택지 원문

| # | 안건 | 선택지 | 각 선택의 결과 |
|:---:|---|---|---|
| **U1** | **Q4 — 프로젝트 위치와 버전관리** | **(a) 현재 폴더에 `git init`, `src/`·`scripts/` 를 루트 바로 아래** (agy 권장)<br>(b) `multi_ai_cli/` 하위 디렉터리 신설<br>(c) git 없이 진행 | (a) 문서와 코드가 한 저장소. 되돌리기 쉬움<br>(b) 문서/코드 분리는 깔끔하나 경로가 한 겹 깊어짐<br>(c) 이력 없음. 실수 복구 불가 |
| **U2** | **Q6 — agy 기본 모델** | **(a) `gemini-3.1-pro-high`** (agy 권장)<br>(b) 실측 후 결정 (codex 권장)<br>(c) `gemini-3.8-flash-high` 유지 | (a) 추론 깊이 확보, 응답 느림<br>(b) 판단 근거 확보. Phase 1 착수는 지연되지 않음(설정값이라 나중에 바꿔도 됨)<br>(c) 빠르지만 깊이 부족 우려 |
| **U3** | **R1 — agy 문맥 전달 방식** | **(a) 문맥 상한을 16,000자로 축소** (agy 권장)<br>(b) `--input-format stream-json` 도입<br>(c) 공급자별 상한 차등 | (a) 단순. Q1과 충돌 없음. 긴 대화에서 문맥 손실<br>(b) 문맥 유지되나 **JSON 파싱 필요 → Q1 확정과 충돌**<br>(c) codex·claude는 stdin으로 크게, agy만 축소. 구현 복잡도 증가하나 손실 최소 |
| **U4** | **2라운드 실행 여부** | **(a) 실행** ← **사용자 결정**<br>(b) 생략 | 사용자가 (a)를 선택했다. **착수 조건은 "세 검토자 모두 더 이상 고칠 것이 없다고 판정"** 이다 |

---

## 5. 반영 계획

확정된 것만. `INTENT.md` 는 수정하지 않는다(양쪽 다 의도를 정확히 파악했다).

| 대상 | 절 | 변경 내용 | 근거 |
|---|---|---|---|
| `SPEC.md` | §1.5 | **구조화 출력 표에 Claude 행 추가** (`--json-schema` / `--output-format json`) | §2.4 기각 — 표 누락이 agy 오판을 유도 |
| `SPEC.md` | §5.3 | 문맥 상한 40,000자 → **16,000자**. 실측 근거와 공급자별 stdin 가능 여부 명시 | R1 (실측) · U3 |
| `SPEC.md` | §6.3 | **stdin 규칙 수정** — "시작 직후 닫는다" → "프롬프트 주입 후 닫는다" | R1 |
| `SPEC.md` | §6.3 | **탐색 우선순위에서 `codex.cmd` 제거.** 네이티브 `codex.exe` → `node.exe` + `codex.js` | R2 |
| `SPEC.md` | §7.2 | codex `-o` 임시 파일을 `%USERPROFILE%\.multi-ai-cli\temp\` 로 격리 | agy 단독 |
| `SPEC.md` | §7.2 | Claude 읽기 전용 프로필에 `--restricted` 확정 | Q3 |
| `SPEC.md` | §7.6 | `transcript.md` 에 기계 판독 경계 마커 규격 추가 | R3 |
| `SPEC.md` | §7.7 | 타임아웃 예시 수정 — **Java 600초, agy 11m** | Q2 (codex 지적) |
| `SPEC.md` | §5.2 / §7.9 | Phase 1 명령 목록 일치 — `/cancel` 포함, 재개 방식 확정 | codex 단독 |
| `SPEC.md` | §7 | **수렴자(Consolidator) 실행 계약 정의** | G1 · R4 |
| `SPEC.md` | §9.2 | Q1·Q2·Q3·Q5 확정 표기. Q4·Q6은 U1·U2로 이관 | §3 |
| `SPEC.md` | §9.4 (신설) | 1차 교차검토 결과 R1~R11 기록 | 전체 |

## 6. 착수 판정

- **Phase 1 착수**: **U1·U3 결정 후 가능.**
- **근거**: 두 검토자 모두 `CONDITIONAL` 이고 막는 사유가 같다. R1(명령행 한계)과 R2(`.cmd` 셸 경유)는 실측으로 확정됐으며 §5.3·§6.3을 고치지 않으면 Phase 1에서 프로세스 기동 자체가 실패한다. U2(agy 모델)는 `config.properties` 값이라 착수를 막지 않는다.

---

# 2라운드 (2026-09-03)

- 원본: `review/codex_review_r2.md`, `review/agy_review_r2.md`
- 프롬프트: `prompts/round2-request.md`

## R2-0. 판정 요약

| 검토자 | 판정 | 남은 blocker |
|---|:---:|---|
| codex | **CONDITIONAL** | 3건 — §7.2 호출 프로필 불일치, transcript 비가역 이스케이프, 프로세스 트리 종료 계약 |
| agy | **GO** | 없음 |

**종합: 갈림. 수렴자는 codex 판정을 채택한다.**

**결정적 근거는 §7.2를 직접 확인한 결과다.** codex가 지적한 대로 §7.2는 확정 결정과 전부 어긋나 있었다.

| 항목 | §7.2 개정 전 표기 | 확정 결정 | 위반 |
|---|---|---|:---:|
| Claude 출력 | `--output-format json` | `D12` — MVP는 text | **O** |
| agy 출력 | `--output-format json` | `D12` — MVP는 text | **O** |
| agy 타임아웃 | `--print-timeout 10m` | `D16` — 660초(11m) | **O** |
| agy 모델 | `<doctor에서 확인한 모델 ID>` | `D13` — `gemini-3.1-pro-high` | **O** |
| Claude 격리 | 없음 | `D17` — `--restricted` 필수 | **O** |
| 프롬프트 전송 | `<prompt>` 인자 | §6.3 — Claude·Codex는 stdin | **O** |

**§7은 문서에서 최우선 구속력을 갖는 절이다.** 구현자가 §7.2를 따르면 §7.0의 확정 결정을 위반한다. agy는 이것을 `[backlog] 잔여 오기` 로 분류했으나, **최우선 절이 확정 결정과 반대로 지시하는 것은 오기가 아니라 구현 차단 사유다.** codex의 분류가 옳다.

> **주목할 점**: agy는 blocker를 "없음"이라 하면서도 backlog에 `§7.2 --output-format json 및 --print-timeout 10m 잔여 오기` 와 `taskkill /T /F 구현 가이드` 를 올렸다. **codex의 blocker 3건 중 2건을 같은 내용으로 발견해놓고 심각도만 다르게 매긴 것이다.** 문제를 못 본 것이 아니라 무게를 다르게 잰 것이다.

## R2-1. A섹션 — 조치 검증 대조

| # | codex | agy | 수렴자 확인 | 최종 |
|:---:|---|---|---|---|
| R1 | 부분 해결 | 해결 | **codex 옳음.** §5.3·§6.3은 고쳤으나 §7.2가 여전히 인자 전달. 16,000자가 "최근 메시지" 상한인지 최종 프롬프트 전체 상한인지도 불명확했다 | **재조치** |
| R2 | 해결 | 해결 | 일치 | **해결** |
| R3 | 부분 해결 | 해결 | **codex 옳음.** `<!--&#32;/msg` 이스케이프는 비가역이고 여는 마커는 미처리 | **재조치** |
| R4 | 부분 해결 | 해결 | **codex 지적 유효.** 수렴자 지명 방식이 검토자를 3인→2인으로 줄여 `INTENT.md` 기본 흐름과 어긋난다 | **backlog (Phase 3)** |
| R5 | 미해결 | 해결 | **codex 옳음.** §8.4 K4가 "완벽 대응 가능"을 유지 중이었고 절차가 없었다 | **재조치** |
| R6 | 부분 해결 | 해결 | codex 지적 유효. §5.3에 원칙만 있고 §7.9에 실행 계약이 없다 | **backlog (Phase 3)** |
| R7 | 해결 | 해결 | 일치 | **해결** |
| R8 | 해결 | 해결 | 일치 | **해결** |
| R9 | 부분 해결 | 해결 | codex 지적 유효. §7.1 클래스 계약 미반영 | **backlog (Phase 3)** |
| R10 | 해결 | 해결 | 일치 | **해결** |
| R11 | 해결 | 해결 | 일치 | **해결** |

**agy가 11건을 전부 `해결`로 판정한 반면 codex는 5건을 `부분 해결`·`미해결`로 판정했고, 수렴자 확인 결과 codex 판정이 다섯 건 모두 옳았다.**

## R2-2. B섹션 — R5 이견 수렴

1라운드에서 갈렸던 유일한 쟁점이 **2라운드에서 좁혀졌다.**

- **agy** (1R: "ProcessHandle로 충분") → **부분 동의로 이동**: "열거와 종료 사이에 분기된 자식이나 분리된 자식을 놓쳐 좀비가 발생할 이론적·실제적 가능성이 존재한다." 대안으로 `taskkill /PID <pid> /T /F` 2단계 폴백 제안.
- **codex**: "`taskkill /T /F` 호출은 Java 라이브러리를 추가하지 않으므로 `D12` 를 위반하지 않는다. (…) 완전 보장이 필요하면 Windows Job Object가 필요하나 Java 17 표준 API에 노출되지 않아 JNA나 네이티브 헬퍼 도입 결정을 별도로 해야 한다."

**양쪽이 같은 해법(`taskkill` 폴백)에 수렴했고, `D12` 위반이 아니라는 판단도 일치했다.** 남은 차이는 심각도뿐이었다 — agy는 구현 방어선, codex는 스펙 계약. **codex 쪽을 채택했다.** `/cancel` 이 §7.10 완료 기준인 이상 보장 수준이 정해지지 않으면 완료 판정이 불가능하다.

## R2-3. 조치

| blocker | 조치 |
|---|---|
| **#1 §7.2 프로필 불일치** | 6개 프로필 전부 재작성. Claude·Codex는 stdin(`codex exec -`), agy는 인자. 출력 전부 `text`. agy `--print-timeout 11m`, `--model gemini-3.1-pro-high`. Claude에 `--restricted` 명시. **§7.2에 "프롬프트 전송 규약" 표 신설** — 16,000자는 최종 조립 프롬프트 **전체** 상한이며, 현재 요청 하나가 상한을 넘으면 **잘라 보내지 않고 거부**한다 |
| **#2 transcript 비가역** | 이스케이프 방식을 폐기하고 **길이 기반 프레이밍**으로 교체. 여는 마커에 `bytes=<UTF-8 바이트 수>` 를 기록하고 정확히 그만큼 읽는다. **본문에 어떤 이스케이프도 하지 않는다.** 닫는 마커는 검증용으로만 사용 |
| **#3 `/cancel` 계약** | §6.3에 **5단계 종료 절차** 신설(자식 우선 → 유예 → 재열거 최대 3회 → `taskkill /T /F` → 생존 PID 오류 표시). **보장 수준을 best-effort로 명시**하고 한계(경쟁 조건·분리된 자식)를 적시. §7.10 완료 기준과 §8.4 K4 판정도 함께 정정 |

## R2-4. backlog (Phase 1 착수를 막지 않음)

| # | 내용 | 시점 |
|:---:|---|---|
| B1 | 수렴자 지명이 검토자를 3인→2인으로 줄이는 문제. 전원 검토 후 별도 호출로 분류할지, 2인 모드로 명시할지 | Phase 3 전 |
| B2 | R6·R9 — 라운드별 문맥 보존·조립 계약을 §7.1·§7.9에 실행 가능한 형태로 | Phase 3 전 |
| B3 | Q5에서 채택한 Claude 중첩 실행 대응(비차단 경고, PID·실행 ID 관리, 세션 환경변수 제거)을 §6.3 또는 §7 실행 계약에 반영 | Phase 1 중 |

## R2-5. 착수 판정

**세 검토자 종합: 착수 가능.**

| 검토자 | 2R 판정 | 조치 후 상태 |
|---|:---:|---|
| codex | CONDITIONAL (blocker 3) | **3건 전부 조치 완료** |
| agy | GO | 조치 완료 |
| Claude (수렴자) | CONDITIONAL (R5) | **R5 조치 완료** |

codex가 제시한 세 blocker의 "최소 조치"를 모두 반영했다. 남은 것은 backlog 3건이며 전부 Phase 3 또는 Phase 1 구현 중 처리 항목이다.

## R2-6. 3라운드 — 수정 검증 패스 (진행)

**돌린다.** `prompts/consolidate.md` 의 "최대 2라운드"는 **반론 토론**의 상한이지 검증의 상한이 아니다. 이번은 반론이 아니라 **수정이 제대로 착지했는지 확인**하는 별개 활동이다.

근거는 이 프로젝트의 실적이다. **수렴자는 1라운드 조치를 "R1~R11 반영 완료"로 판정했으나, 2라운드에서 codex가 5건이 부분 해결이고 최우선 절(§7.2)이 확정 결정과 정반대임을 찾아냈다.** 자체 판정이 이미 한 번 틀렸고, 이번 수정은 1라운드 때보다 규모가 크다.

| 수정 | 성격 | 검증 필요성 |
|---|---|---|
| §7.2 프로필 6개 + 전송 규약 표 | 전면 재작성 | 확정 결정 5개와의 정합성을 아무도 확인하지 않았다 |
| §7.6 길이 기반 프레이밍 | **규격 교체** | **수렴자가 새로 설계했다.** 방향만 제시받았고 구체안은 미검증 |
| §6.3 5단계 종료 절차 | 신설 | 보장 수준과 한계 기술의 정직성을 확인해야 한다 |

**요청서: `prompts/round3-request.md`** — 범위를 B1·B2·B3으로 좁혔고, 확정 사항 재논쟁과 backlog 재제기를 금지했다. B2에는 "적극적으로 깨뜨리고 반례를 제시하라"를 명시했다.

### 발행 전 수렴자 자체 수정 1건

round3 요청서를 쓰면서 §7.6 재동기화 절차의 결함을 발견해 먼저 고쳤다.

- **문제**: 손상 발생 시 "다음 `<!-- msg ` 헤더를 찾아 계속한다"고만 했다. 그런데 **손상된 메시지의 본문 안에도 `<!-- msg ` 가 들어 있을 수 있어** 본문 한가운데를 새 메시지 시작으로 오인한다.
- **조치**: 후보를 **검증에 통과한 것만 채택**하도록 4개 조건을 신설했다 — ① 줄 시작 위치 ② 헤더 파싱 성공 ③ 닫는 마커 일치 ④ **`id` 단조 증가** (본문 속 가짜 마커는 이전 `id` 를 재사용하므로 걸러진다).

이 결함은 codex·agy가 아직 보지 못한 상태에서 나왔다. **B2를 검증 대상에 넣은 판단이 옳았다는 방증이다.**

### 절차 자체의 보강

이 상황이 `consolidate.md` 의 빈틈을 드러냈다. **반론 라운드와 수정 검증 패스를 구분하는 절**을 `prompts/consolidate.md` 와 `SPEC.md` §7.5-1 양쪽에 추가했다. Phase 3 자동화 시에도 같은 구분이 필요하다 — 그러지 않으면 자동 파이프라인이 똑같이 검증 없이 종료한다.

---

# 3라운드 — 수정 검증 패스 (2026-09-03)

- 원본: `review/codex_review_r3.md`, `review/agy_review_r3.md`
- 프롬프트: `prompts/round3-request.md` (범위: B1·B2·B3만)

## R3-0. 판정 요약

| 검토자 | 판정 | B1 | B2 | B3 |
|---|:---:|:---:|:---:|:---:|
| codex | CONDITIONAL | 해결 | 부분 해결 | 부분 해결 |
| agy | GO | 해결 | 해결 | 해결 |

**종합: 3라운드를 돌린 판단이 옳았다.**

**두 검토자가 독립적으로 B2의 같은 결함 두 개를 찾아냈다.** 수렴자가 새로 설계한 규격이었고 아무도 검증하지 않은 상태였다.

## R3-1. B1 — §7.2 호출 프로필: **양쪽 `해결`**

7개 확인 항목 전부 O. codex: "§7.2와 §7.0 D12·D13·D15·D16·D17, §6.3의 stdin 규칙, §7.7의 Java 600초/agy 660초 사이에 남은 모순은 없다." agy도 동일.

`codex exec -` 의 `-` 는 agy가 `codex exec --help` 를 직접 재확인해 유효성을 확증했다.

## R3-2. B2 — 길이 기반 프레이밍: **결함 2건 발견, 둘 다 합의**

### 결함 ① 디스크 개행 규격 미정 (양쪽 지적)

- codex: "헤더의 '개행'이 LF 한 바이트인지 Windows CRLF 두 바이트인지 명시되지 않았다. 플랫폼 기본 개행을 사용하면 검증 규칙의 `\n<!-- /msg` 와 다른 바이트열이 생성될 수 있다."
- agy: "`bytes` 계산 및 닫는 마커 매칭에서 1바이트 오차가 발생할 수 있다."

**타당하다.** 규격에 개행 종류가 없었다.

### 결함 ② 재동기화가 보장이 아닌 휴리스틱 (양쪽이 같은 반례 제시)

두 검토자가 서로 못 본 상태에서 **같은 구조의 반례**를 만들었다 — 손상된 본문 안에 완결된 가짜 프레임이 줄 시작부터 들어 있으면 조건 1~4를 전부 통과한다.

- codex: "따라서 재동기화는 정상 프레임을 보장하는 절차가 아니라 **휴리스틱**이다."
- agy: "가짜 마커가 진짜 메시지 2로 오인 복원된다. 그 뒤에 실제 진짜 메시지 2가 등장하면 ID 충돌로 진짜 메시지가 버려진다."

**타당하다.** `id > last_id` 조건은 미래 `id` 를 가진 가짜 프레임을 막지 못한다.

### 조치

| 결함 | 조치 |
|---|---|
| ① 개행 | **디스크 개행을 LF(`0x0A`)로 고정.** `println`·`PrintWriter` 금지, 바이트 스트림에 `\n` 직접 기입 |
| ① 인코딩 | 읽기 시 **strict 디코딩**(`CodingErrorAction.REPORT`), 실패 시 `CORRUPT`. 원시 바이트는 `runs/` 원본에 보존되므로 소실 없음 |
| ② 조건 강화 | `id > last_id` → **`id == last_id + 1`** (건너뛴 값 차단) + **`id < room.properties` 의 `next_id`** (미래 id 차단). `next_id` 를 `room.properties` 에 유지 |
| ② 약속 하향 | "손상 뒤 나머지를 **정상 복원한다**" → **"복구를 시도하며 복구분은 `SUSPECT` 로 표시한다"**. 재동기화가 best-effort 휴리스틱임을 명시 |

**조건 강화는 반례를 좁히지만 없애지 못한다** — `id` 가 정확히 `last_id + 1` 이고 `next_id` 미만인 가짜 프레임은 여전히 통과한다. 그래서 약속 자체를 낮췄다. **손상되지 않은 파일의 무손실 왕복은 재동기화와 무관하며 길이 프레이밍만으로 보장된다.**

### 수렴자 실측 — 종이 논쟁 대신 실행

프레이밍 왕복을 Java 17 표준 라이브러리만으로 구현해 적대적 본문으로 검증했다.

```
written=8 restored=8
  RESULT: all 8 bodies byte-identical round-trip OK
file contains CR byte: yes (from body content only)
```

| 케이스 | 결과 |
|---|:---:|
| 본문에 닫는 마커 `<!-- /msg id=0002 -->` | **통과** — `bytes` 로 건너뛰고 진짜 마커 도달 |
| 본문에 완결된 가짜 프레임(여는+본문+닫는) | **통과** |
| 구 이스케이프 흔적 `<!--&#32;/msg` | **통과** — 이스케이프를 안 하므로 무의미 |
| 코드펜스 / 널 문자 / 빈 본문 | **통과** |
| 이모지·결합문자 / 본문 내 CRLF | **통과** |
| 프레이밍 자체의 CRLF 혼입 | **없음** — 전부 LF |

**B2의 핵심 주장(임의 본문의 무손실 왕복)은 실측으로 확정됐다.** 재동기화 휴리스틱의 한계는 별개이며 위에서 약속을 낮췄다.

> 검증 소스는 heredoc을 거치며 한글이 U+FFFD로 손상됐다. 따라서 확정된 것은 **바이트 동일성**이며 한글 자체의 왕복은 별도 스모크 테스트(§7.10)에서 확인한다.

## R3-3. B3 — 프로세스 트리 종료: **codex 단독으로 순서 결함 발견**

agy는 "5단계는 Java 코드로 즉시 옮길 수 있을 만큼 구체적이며 모호한 단계가 없다"며 `해결` 판정했다. **codex가 순서 결함을 잡아냈고, 수렴자가 §6.3 원문을 확인한 결과 codex가 맞다.**

> codex: "1단계가 부모까지 종료하므로 3단계에서 `parent.descendants()` 를 다시 열거할 기준 프로세스가 사라지고, 4단계의 `taskkill /PID <parent> /T /F` 도 이미 종료된 PID라 실패할 수 있다."

개정 전 1단계는 "자식부터 `destroyForcibly()` 하고 **마지막에 부모를 종료**한다"였다. 부모가 죽으면 재열거도 `taskkill /T` 도 불가능하다. **"마지막"이 1단계의 끝이 아니라 전체 절차의 마지막이어야 했다.**

### 조치 — 루프 구조로 재작성

```text
[반복 루프: 최대 3회]
  1. parent.descendants() 열거      ← 부모는 건드리지 않는다
  2. 깊은 것부터 destroyForcibly()
  3. 유예 2초 대기
  4. 재열거 → 생존 자손 없으면 탈출
[루프 종료 후]
  5. 부모가 살아 있으면 taskkill /PID <parent> /T /F
  6. taskkill 부재·실패 시에만 parent.destroyForcibly()
  7. 관측 핸들 종료 확인 → 생존 PID 오류 표시
```

codex의 4번 지적(보고 범위)도 반영했다 — **"생존 PID 없음"은 "전부 종료됐다"가 아니라 "추적한 범위에서 남은 것이 없다"** 는 뜻임을 명시하고 사용자 표시 문구도 그렇게 쓴다.

## R3-4. 3라운드가 준 것

| | 값 |
|---|---|
| 발견된 결함 | **3건** (B2 개행, B2 재동기화, B3 순서) — 전부 blocker급 |
| 양쪽 독립 합의 | 2건 (B2 결함 둘) |
| 단독 발견 | 1건 (B3 순서 — codex) |
| 수렴자가 사전 자체 발견 | 1건 (재동기화 가짜 마커 — 요청서 작성 중) |

**3라운드를 생략했다면 이 셋을 안고 구현에 들어갔다.** 특히 B3 순서 결함은 `/cancel` 이 첫 실행부터 오작동했을 것이다.

**절차적 교훈**: 수렴자가 **새로 설계한 부분**은 반드시 검증 패스를 거쳐야 한다. B1(확정 결정을 옮겨 적은 것)은 양쪽 다 한 번에 `해결`이 났고, B2·B3(수렴자 신규 설계)에서만 결함이 나왔다. `prompts/consolidate.md` 와 `SPEC.md` §7.5-1의 검증 패스 조건에 이 기준이 이미 반영돼 있다.

## R3-5. 착수 판정

| 검토자 | 3R 판정 | 조치 후 |
|---|:---:|---|
| codex | CONDITIONAL (2건) | **2건 전부 조치.** 제시한 "최소 수정"을 그대로 반영 |
| agy | GO | 조치 완료 |
| Claude (수렴자) | — | B2 실측 통과, B3 순서 결함 확인·수정 |

**남은 blocker 0건. Phase 1 착수 가능.**

이번 조치는 **검토자들이 명시한 "최소 수정"을 그대로 구현한 것**이고 수렴자의 신규 설계가 포함되지 않았다.

## R3-6. 4라운드 — 착지 확인 패스 (진행)

**사용자 결정: 진행한다.** 종료 조건은 **codex·agy 양쪽이 `GO`** 다.

요청서 `prompts/round4-request.md` — 범위를 3라운드보다 더 좁혀 **C1·C2·C3 세 지점의 착지 여부만** 본다.

| # | 3R 지적 | 처방 → 반영 |
|:---:|---|---|
| C1 | 개행 규격 미정의 | LF 고정 + strict 디코딩 정책 → §7.6 |
| C2 | 재동기화가 휴리스틱 | 조건 강화 + **약속 하향** → §7.6 |
| C3 | 종료 5단계 순서 결함 | 루프 구조 재작성 → §6.3 |

### 수렴 전망과 종료 조건

라운드별 결함 발생 위치를 보면 패턴이 뚜렷하다.

| 라운드 | 새 결함 | 어디서 나왔나 |
|:---:|:---:|---|
| 2R | 3건 | 수렴자가 확정 결정을 §7.2에 **내리지 않아서** + 수렴자 신규 설계(이스케이프) |
| 3R | 3건 | **전부 수렴자 신규 설계**(프레이밍 2건, 종료 절차 1건). 확정 결정을 옮겨 적은 B1은 양쪽 한 번에 `해결` |
| 4R | ? | **수렴자 신규 설계 없음.** 검토자 처방을 그대로 구현 |

**결함은 수렴자가 새로 설계한 곳에서만 나왔다.** 4라운드 수정에는 그런 부분이 없으므로 수렴이 예상된다.

**다만 "양쪽 GO까지 반복"에는 발산 위험이 있다.** 검토자는 무언가를 찾도록 요청받으면 대체로 찾아내고, 수정할 때마다 새 표면이 생긴다. 아래 규칙으로 종료를 보장한다.

1. **매 라운드 범위를 직전 지적으로만 좁힌다.** 확정 사항·backlog·이전 라운드 `해결` 항목의 재개를 금지한다. 범위 밖 발견은 `[범위 밖]` 으로 분리해 판정에서 뺀다.
2. **같은 지점에서 두 라운드 연속 blocker가 나오면 문서 수정을 멈추고 코드로 검증한다.** 3라운드 B2가 그 사례였다 — Java 20줄로 왕복시키니 논쟁이 끝났다.
3. **새 blocker가 수렴자의 신규 설계에서 나왔는지 확인한다.** 그렇다면 설계를 문서로 밀지 말고 프로토타입으로 옮긴다.
4. 범위 밖 발견이 정말 중요하면 blocker가 아니라 **backlog**로 받아 Phase 1 이후에 처리한다.

---

# 4라운드 — 착지 확인 (2026-09-03)

- 원본: `review/codex_review_r4.md`, `review/agy_review_r4.md`

## R4-0. 판정 요약

| 검토자 | 판정 | C1 개행 | C2 재동기화 | C3 종료 순서 |
|---|:---:|:---:|:---:|:---:|
| codex | CONDITIONAL (1건) | 해결 | **미해결** | 해결 |
| agy | **GO** | 해결 | 해결 | 해결 |

**C1·C3은 양쪽 다 `해결`.** codex가 5개 확인 항목 전부 O를 주며 "3라운드 최소 수정과 실제 반영 내용이 일치한다"고 확인했다.

## R4-1. codex 단독 blocker — 문서 내 자기모순

> codex: "재동기화 설명은 복구분을 `SUSPECT` 로 표시하고 best-effort라고 낮췄지만, **같은 절의 「읽기 절차」 4번은 여전히 '나머지 메시지는 정상 복원한다'라고 단정한다.** 한 절 안에서 기존 보장과 하향된 보장이 동시에 존재한다."

**타당하다.** 수렴자가 재동기화 블록의 약속만 낮추고 읽기 절차 본문을 함께 고치지 않았다. §7은 최우선 구속력을 갖는 절이므로 상반된 두 계약이 공존하면 구현자가 어느 쪽을 따를지 결정할 수 없다.

**조치**: codex가 제시한 문구로 그대로 교체 — "나머지 메시지는 정상 복원한다" → **"재동기화를 시도하고 복구 후보를 `SUSPECT` 로 표시한다"**.

## R4-2. 실측 추가 — codex가 짚은 미검증 구간

> codex: "잘린 UTF-8의 strict 디코딩 실패 처리와 손상 뒤 재동기화 경로도 이 테스트에서는 실행하지 않았다."

맞는 지적이라 두 경로를 실제로 구현해 돌렸다.

**T1 — 잘린 UTF-8 (`0xED 0x95`, '한'의 앞 2바이트)**

```
id=1 state=OK body=normal
id=2 state=CORRUPT(decode)      ← strict 디코딩이 정상 검출
id=3 state=SUSPECT body=after   ← 이후 복구
```

**설계대로 동작한다.**

## R4-3. ⚠ 실측이 3라운드 처방의 결함을 드러냈다

**T2 — 손상된 헤더 + 본문 속 완결된 가짜 프레임(id=3)**

```
== 엄격 조건 (id == last_id + 1) — 3R 처방 ==
   id=1 state=OK body=first
   id=-1 state=CORRUPT
   (끝 — 진짜 id=3 복구 실패, 이후 전부 유실)

== 완화 조건 (id > last_id && id < next_id) ==
   id=1 state=OK body=first
   id=-1 state=CORRUPT
   id=3 state=SUSPECT body=FAKE      ← 가짜가 채택됨
   id=-1 state=CORRUPT
```

| 조건 | 가짜 프레임 | 진짜 후속 메시지 |
|---|---|---|
| `id == last_id + 1` (3R 처방) | 거부 ✓ | **유실. 복구 0건** ✗ |
| `id > last_id` | SUSPECT로 채택 ✗ | 복구 시도 ✓ |

**3라운드에서 agy가 제안하고 codex가 수용한 강화 조건이 오히려 나쁘다.** 손상된 메시지가 통째로 유실되면 `id` 가 건너뛰므로, `last_id + 1` 을 요구하면 어떤 후보와도 맞지 않아 **그 뒤 전부를 잃는다.**

가짜 프레임은 "AI가 완결된 프레임을 본문에 출력" + "파일 손상"이 동시에 필요한 희귀 조합이지만, **"손상 뒤 전량 유실"은 모든 손상에서 발생한다.**

**조치**: 조건 4를 `id > last_id` 로 되돌리고, 실측 근거와 함께 "어느 조건을 써도 재동기화는 완전하지 않다. 실패 모드를 고르는 문제"임을 §7.6에 명시했다.

> **이번 라운드의 교훈**: 두 검토자가 합의한 처방도 틀릴 수 있다. **종이 위 추론과 실행 결과가 갈렸고, 실행이 맞았다.** §R3-6의 종료 규칙 2번("같은 지점에서 두 라운드 연속 blocker가 나오면 문서 수정을 멈추고 코드로 검증한다")이 그대로 작동한 사례다.

## R4-4. 5라운드 (진행)

요청서 `prompts/round5-request.md` — 확인 항목 **두 개뿐**이다.

| # | 내용 |
|:---:|---|
| **E1** | 4R blocker 조치 — 읽기 절차 4번 문구 교체. §7.6 안에 다른 보장 문장이 남아 있지 않은지 |
| **E2** | **3R 처방 되돌림** — 조건 4 완화. 실측 해석과 판단이 타당한지, `D12` 안에서 두 실패 모드를 모두 피하는 제3의 조건이 있는지 |

E2는 **검토자들의 처방을 수렴자가 뒤집은 것**이므로 실측 설계 자체의 결함을 지적할 기회를 명시적으로 열어뒀다.

### 진행 상황

| 라운드 | codex | agy | 새 결함 | 출처 |
|:---:|:---:|:---:|:---:|---|
| 1R | CONDITIONAL | CONDITIONAL | 11 | 원 스펙 |
| 2R | CONDITIONAL(3) | GO | 3 | 확정 결정 미반영 + 수렴자 신규 설계 |
| 3R | CONDITIONAL(2) | GO | 3 | **전부 수렴자 신규 설계** |
| 4R | CONDITIONAL(1) | **GO** | 1 + **실측 1** | 수렴자 누락 1줄 + **3R 처방 오류** |
| 5R | ? | ? | — | — |

**codex의 blocker가 3 → 2 → 1 로 줄고 있다.** agy는 2R부터 계속 `GO`. 수렴 중이다.

---

# 5라운드 — 게이트 종료 (2026-09-03)

- 원본: `review/codex_review_r5.md`, `review/agy_review_r5.md`

## R5-0. 판정

| 검토자 | 판정 | E1 문구 정합 | E2 조건 되돌림 |
|---|:---:|:---:|:---:|
| codex | **GO** | 해결 | **동의** |
| agy | **GO** | 해결 | **동의** |

**양쪽 `GO`, blocker 0건. 사용자가 정한 종료 조건(codex·agy 모두 GO)을 충족했다.**

## R5-1. E1 — 문구 정합

양쪽이 §7.6 전수 점검 후 잔여 보장 문장이 없음을 확인했다.

> codex: "§7.6 전체를 확인했다. '정상 복원한다'는 기존 보장 문장은 남아 있지 않으며, 해당 표현은 §7.6 후반에서 **폐기된 약속을 인용해 부정하는 문맥으로만** 나타난다."

계약이 하나로 정리됐다 — **손상되지 않은 파일은 `bytes` 프레이밍으로 무손실 왕복 보장, 손상 이후는 best-effort이며 복구분은 `SUSPECT` 표시.**

## R5-2. E2 — 되돌림에 양쪽 동의

**자기 처방이 뒤집힌 쪽(agy가 제안, codex가 수용)이 둘 다 되돌림에 동의했다.**

> codex: "직전 정상 ID가 1이고 손상으로 ID 2 프레임을 잃었다면 이후 실제 ID 3도 `last_id + 1` 조건을 만족하지 못한다. 후보를 채택하지 못하므로 `last_id` 가 계속 1에 머물고, **뒤의 모든 정상 ID도 연쇄적으로 거부된다.**"

> agy: "'단 1건의 손상으로 뒤따르는 모든 정상 대화가 영구 유실되는 전량 유실 위험'이 압도적으로 치명적이다. 실패 모드 선택 관점에서 `id > last_id` 로 되돌리고 `SUSPECT` 경고를 노출하는 정책이 **올바른 엔지니어링 선택**이다."

**제3의 조건: 양쪽 다 "없음".**

> codex: "후보 프레임과 실제 프레임이 같은 문법을 사용할 수 있으면 후보 속성만으로 양자를 항상 구분할 수 없다. 별도의 신뢰 가능한 오프셋·인증 정보는 Java 17 표준으로 만들 수 있지만 **저장 계약 자체를 바꾸는 별도 설계**이지, 두 실패 모드를 없애는 추가 후보 조건은 아니다."

### 수렴자가 정정할 점

codex가 실측 해석의 범위를 정확히 짚었다.

> "제시된 실측에서 **완화 조건이 진짜 ID 3을 복구한 것은 아니고 가짜 ID 3을 채택했다.**"

**맞다.** T2는 *엄격 조건이 아무것도 복구하지 못한다*는 것만 보였고, *완화 조건이 진짜 메시지를 복구한다*는 것은 보이지 않았다(가짜가 먼저 걸렸다). 되돌림의 근거는 실측 하나가 아니라 **"손실로 `id` 가 건너뛰면 `last_id+1` 은 영원히 맞지 않는다"는 추론**이며, 이는 codex·agy가 E2-1에서 독립적으로 검증했다. 문서가 주장하는 것도 정확 복구가 아니라 **표시된 best-effort** 이므로 계약과 어긋나지 않는다.

## R5-3. 최종 게이트

| 검토자 | 최종 판정 |
|---|:---:|
| codex (GPT-5 Codex) | **GO** |
| agy (Gemini 3.8 Flash High) | **GO** |
| Claude (수렴자) | **GO** |

**blocker 0건. Phase 1 착수 가능.**

## R5-4. 5라운드 전체 결산

| 라운드 | codex | agy | 새 결함 | 결함의 출처 |
|:---:|:---:|:---:|:---:|---|
| 1R | CONDITIONAL | CONDITIONAL | 11 | 원 스펙 |
| 2R | CONDITIONAL(3) | GO | 3 | 확정 결정 미반영 + 수렴자 신규 설계 |
| 3R | CONDITIONAL(2) | GO | 3 | **전부 수렴자 신규 설계** |
| 4R | CONDITIONAL(1) | GO | 1 + 실측 1 | 수렴자 누락 1줄 + **3R 처방 오류** |
| 5R | **GO** | **GO** | 0 | — |

**총 19건.** codex blocker가 3→2→1→0 으로 단조 감소했다.

### 이 예행연습이 Phase 3에 남긴 것

| 발견 | 근거 |
|---|---|
| **수렴자의 신규 설계에서만 결함이 나온다** | 확정 결정을 옮겨 적은 항목(2R B1)은 양쪽 한 번에 `해결`. 3R 결함 3건은 전부 수렴자 신규 설계 |
| **두 검토자가 합의한 처방도 틀릴 수 있다** | 3R의 `id == last_id + 1` — 양쪽 합의했으나 실측에서 반대 결과 |
| **종이 추론과 실행이 갈리면 실행이 맞다** | 4R T2. 문서 논쟁 대신 Java 20줄이 결론을 냈다 |
| **반론 라운드(≤2)와 검증 패스는 별개다** | 3~5R은 전부 검증 패스였고 매번 결함이 나왔다. 2라운드에서 끊었다면 19건 중 7건을 안고 착수했을 것 |
| **범위를 좁혀야 수렴한다** | 라운드마다 직전 지적으로만 범위를 제한했고 blocker가 단조 감소했다 |

이 다섯 가지를 `SPEC.md` §7.5-1(수렴자 실행 계약)과 `prompts/consolidate.md` 에 반영했다. Phase 3 구현은 이 계약을 자동화하는 것이다.
