# INTENT — 무엇을, 왜 만드는가

> 이 문서가 프로젝트의 최상위 기준이다. 다른 문서와 충돌하면 이 문서가 이긴다.
> 분량이 짧은 것은 의도적이다. 검토자와 구현자는 **이 문서를 먼저 끝까지 읽고** `SPEC.md`로 넘어간다.
>
> 최종 갱신: 2026-09-03

---

## 1. 한 문장

**Claude · Codex · Gemini(agy) 세 AI가 한 채팅방에 참여자로 들어와 있고, 사용자가 던진 안건에 대해 각자 독립적으로 답한 뒤 서로의 답을 읽고 쟁점을 좁혀가는 독립 CLI 애플리케이션**을 만든다.

---

## 2. 최종적으로 원하는 것 — 교차검증 수렴

이 프로젝트의 존재 이유는 "AI 3개를 한 번에 부르는 것"이 아니다. 그것은 수단이다.

**진짜 목적은 혼자 판단하기 어려운 설계 결정을 세 모델에게 던져서, 무엇이 합의됐고 무엇이 갈렸고 무엇이 아직 미해결인지 좁혀내는 것이다.**

원하는 최종 흐름:

```text
사용자가 안건을 던진다
  → 세 AI가 서로 못 본 상태에서 독립적으로 답한다
  → 답을 대조해 [합의 / 이견 / 단독 지적 / 미해결] 로 분류한다
  → 이견이 있으면 상대의 반박을 첨부해 한 번 더 묻는다 (최대 2라운드)
  → 끝까지 안 좁혀진 것만 사용자에게 결정을 요청한다
```

`SPEC.md` §7.9의 **Phase 3(구조화 수렴)** 이 이것이고, 이 프로젝트의 진짜 결승선이다.
Phase 1(채팅방)과 Phase 2(실행 프로필)는 거기까지 가기 위한 토대다.

> **검토자에게**: 설계를 평가할 때 "Phase 3에 도달할 수 있는 구조인가"를 기준으로 삼아라.
> Phase 1만 놓고 "이 정도면 그냥 터미널 3개 띄우면 되지 않나"라고 판단하지 마라. 그건 목적이 아니다.

### 이 문서 자체가 근거다

지금 이 요구사항은 가상이 아니다. **사용자는 이미 그 작업을 손으로 하고 있다.**

`SPEC.md`와 `BACKGROUND.md`는 codex가 초안을 쓰고, agy가 실측 검증해 쟁점(K1~K6)을 반영하고, Claude가 교차 확인해 결함(F1~F10)을 정리한 결과물이다. 매번 프롬프트를 손으로 만들고, 답을 손으로 대조하고, 모순을 손으로 찾아내고 있다.

**이미 수동으로 돌아가고 있는 이 절차를 자동화하는 것**이 이 프로젝트다.

---

## 3. 왜 이런 제약이 붙었는가

설계상의 제약은 취향이 아니라 아래 세 가지에서 전부 파생됐다. 제약을 건드리는 제안을 하려면 이 근거부터 반박해야 한다.

### 3.1. 구독료를 이미 3개 내고 있다 → API 키 과금은 이중 지불이다

Claude · Codex · Gemini 세 구독을 모두 결제 중이다. 따라서 **각 CLI에 이미 로그인된 구독 인증을 그대로 타야 하고, API 키로 별도 과금되는 경로는 전부 탈락**이다.

이것이 LangGraph · CrewAI · MetaGPT · AutoGen · zen-mcp-server 본체를 **기술적 우열과 무관하게** 배제한 유일한 기준이다(`BACKGROUND.md` §2-D). 선택지를 좁힌 것은 성능이 아니라 과금 구조다.

> **금지**: API 키를 요구하는 프레임워크·라이브러리·서비스를 대안으로 제시하지 마라. 이미 검토했고 이 이유로 탈락했다.

### 3.2. 역할을 제품이 정하면 안 된다

`planner` / `implementer` / `reviewer` 같은 고정 역할을 **애플리케이션에 내장하지 않는다.**

오늘은 Gemini가 계획하고 Claude가 구현할 수 있고, 내일은 정반대일 수 있다. 역할은 그때그때 **사용자가 프롬프트로** 정한다. 제품은 누가 무엇을 하는지 해석하지도, 강제하지도 않는다.

`SPEC.md` §7.5가 이것이고, §7.10 완료 기준에 "제품이 참여자별 역할을 임의로 강제하지 않는다"가 검수 항목으로 들어가 있다.

> **금지**: 역할 프리셋을 내장하자는 제안. PAL MCP의 `planner`/`codereviewer` 프리셋을 그대로 가져오자는 제안.

### 3.3. 누가 말했는지 항상 보여야 한다

응답·실행 상태·오류가 **어느 AI의 것인지 언제나 즉시 식별**돼야 한다. 한 AI의 출력 블록에 다른 AI의 출력이 섞이면 안 된다.

이것이 Maestro 같은 별도 UI 오케스트레이터를 탈락시킨 사유다(`BACKGROUND.md` §2-C).

---

## 4. 만들지 않는 것 (Non-goals)

아래는 **이미 검토했고 명시적으로 폐기한** 방향이다. 재제안하지 마라. 재제안하려면 §3의 근거를 먼저 반박해야 한다.

| # | 만들지 않는 것 | 폐기 사유 |
|:---:|---|---|
| N1 | **Claude Code 내부 스킬** (`~/.claude/skills/ask-*`, `tri-review`) | 초기안이었으나 §4.2에서 폐기. 전역 Claude 설정을 수정하지 않는다. `multi_ai_cli`가 직접 호스트다. 기록은 `ARCHIVE.md` |
| N2 | **MCP 서버 등록** (`codex mcp-server` 직결, agy MCP 브리지) | 툴셋 중복과 컨텍스트 소모. 조사 기록은 `BACKGROUND.md` §2-B |
| N3 | **API 키 기반 프레임워크** | §3.1 — 이중 과금 |
| N4 | **고정 역할 파이프라인** | §3.2 — 역할은 프롬프트가 정한다 |
| N5 | **구 `gemini` CLI 사용** | 2026-06-18 개인 티어 서비스 종료. `IneligibleTierError`. **`agy`만 쓴다** (`SPEC.md` §1.2) |
| N6 | **공급자 세션 재개 기능 의존** (`codex resume --last`, `agy -c`) | 세 AI 세션을 각자 관리하면 동기화가 깨진다. 앱이 저장한 채팅 기록이 유일한 진실이다 (`SPEC.md` §5.3, §8.4 K3) |
| N7 | **권한 우회 플래그** (`--yolo`, `--dangerously-*`) | 기본은 전 참여자 읽기 전용. 쓰기는 `/run --write` 1회만 |
| N8 | **자동 커밋·푸시** | 어떤 경우에도 사용자 지시 없이 하지 않는다. 대상 프로젝트 다수가 SVN이라 롤백이 어렵다 |

---

## 5. 협업이 실제로 일어나는 방식

단순 동시 호출이 아니다. **라운드 경계**가 설계의 핵심이다.

- **같은 라운드**: 세 AI는 서로의 답을 **보지 못한다**. 동일한 사용자 메시지와 이전까지의 채팅 기록만 받는다.
- **다음 라운드부터**: 직전 라운드에서 다른 AI가 **사용자에게 공개한 최종 답변만** 공동 문맥에 들어간다.
- **절대 넘기지 않는 것**: 다른 AI의 stderr, 내부 추론 스트림, 불완전한 툴 호출 로그.

이유는 두 가지다. 먼저 답하는 AI를 보고 **동조하는 것을 막아야** 하고(§8.4 K5), 동시에 원시 로그가 섞여 **모델 간 환각이 증폭되는 것**을 막아야 한다.

---

## 6. 현재 상태와 다음 할 일

| 항목 | 상태 |
|---|---|
| 요구사항·환경 실측 | 완료 (`SPEC.md` §0~§1) |
| 기존 솔루션 조사 | 완료 (`BACKGROUND.md`) |
| 설계 스펙 | 완료 (`SPEC.md` §4~§8) |
| agy 독립 검토 | 완료 (`SPEC.md` §8.4, K1~K6) |
| Claude 교차 확인 | 완료 (`SPEC.md` §9.1, F1~F10) |
| 1차 교차검토 (codex·agy) | 완료 (`REVIEW_REPORT.md`). 양쪽 `CONDITIONAL` |
| 미해결 쟁점 Q1·Q2·Q3·Q5 | **확정** (`SPEC.md` §9.2) |
| 사용자 결정 U1~U4 | 완료 (`SPEC.md` `D13`~`D18`) |
| 교차검토 1~5라운드 | **완료. 5라운드에서 codex·agy 양쪽 `GO`** (`REVIEW_REPORT.md`). 총 19건 조치 |
| **Phase 1 구현** | **← 현재 위치. blocker 0건, 착수 가능** |

**codex·agy·Claude 세 검토자 모두 `GO`. blocker 0건. Phase 1 착수 가능하다.** 남은 backlog 3건은 `REVIEW_REPORT.md` §R2-4에 있으며 Phase 3 또는 구현 중 처리한다.

---

## 7. 문서 지도

| 파일 | 내용 | 언제 읽나 |
|---|---|---|
| `INTENT.md` | 이 문서. 목적·동기·금지사항 | **항상 먼저** |
| `SPEC.md` | 요구사항, 환경 실측, 설계 스펙, 미해결 쟁점 | 구현·검토 시 |
| `BACKGROUND.md` | 기존 솔루션 조사, 선택 근거, 참고 링크 | "왜 그건 안 썼나" 물을 때만 |
| `ARCHIVE.md` | **폐기된 초기안.** 실행 기준 아님 | 원칙적으로 읽지 않는다 |

> **⚠ `ARCHIVE.md` 경고**
> 폐기안임에도 복붙 가능한 셸 명령·완성된 JSON 스키마·체크박스를 갖추고 있어 **현행 스펙보다 실행 가능해 보인다.** 실제로 이 함정은 이미 한 번 발동했다 — `SPEC.md` §6.3이 `< /dev/null` 을 금지하는데 폐기안은 "필수"라고 규정했고, 같은 사람이 두 문서를 동시에 붙들지 못했다.
> **검토자에게는 `ARCHIVE.md`를 제공하지 않는다.**

---

## 8. 판단 기준 (충돌 시 우선순위)

```text
INTENT.md  >  SPEC.md §7  >  SPEC.md §4~§6·§8·§9  >  SPEC.md §0~§1  >  BACKGROUND.md  >  ARCHIVE.md(참고 불가)
```
